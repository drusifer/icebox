include Makefile.bob
# ICEbox repo development entry point.
# Includes Icebox.mk which is the distributable user-facing file.
# Run `make build` to build the image, `make test` to run the test suite.
include Icebox.mk
